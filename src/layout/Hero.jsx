import Header from "./Header";

const Hero = () => {
  return (
    <div className="hero">
      <div className="hero-content">
        <Header />
      </div>
    </div>
  );
};

export default Hero;
